<h1>Slow Notes 🐌</h1>

<h2>Routes we'll be looking at</h2>
<ul>
    <li>
        Your todos
        <% if User.first %> 
            (<%= link_to "for first user", user_todos_path(User.first) %>)
        <% else %>
            (ℹ️ You need to run <code>rails sample_data:generate</code>)
        <% end %>
    </li>
    <li>Most prolific users</li>
</ul>

<h2>Scaffolded routes</h2>
<ul>
    <li><%= link_to "Users", users_path %></li>
    <li><%= link_to "Todos", todos_path %></li>
    <li><%= link_to "Comments", comments_path %></li>
</ul>